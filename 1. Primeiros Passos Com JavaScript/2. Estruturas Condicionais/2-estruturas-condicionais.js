/*
    Bootcamp Orange Tech+ 
    Curso Primeiros Passos Com JavaScript
    Aula 2: Estruturas Condicionais 
*/


/*
~Boolean~
verdadeiro/true 1
falso/false 0

- colocando um ! na frente de uma variável, o boolean converte para o oposto

    exemplo:

    const numero = 3;
    const numeroPar = (numero % 2) === 0;

    if (!numeroPar) { 
    console.log('Impar')
    }

~IF, ELSE IF e ELSE~
if (se condição verdadeira...faz tal coisa)
else if (se o if não for verdadeiro, executa tal coisa)
else (se nenhuma das condições anteriores for verdadeira... só resta fazer tal coisa)


atalho para abrir o terminal: Ctrl + Shift + aspas

*/
